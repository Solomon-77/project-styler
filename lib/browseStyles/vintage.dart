import 'package:flutter/material.dart';

class Vintage extends StatelessWidget {
  const Vintage({super.key});

  @override
  Widget build(BuildContext context) {
    return AppBar(
      title: Text("Vintage"),
    );
  }
}
