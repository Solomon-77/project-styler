import 'package:flutter/material.dart';

class Bohemian extends StatelessWidget {
  const Bohemian({super.key});

  @override
  Widget build(BuildContext context) {
    return AppBar(
      title: Text("Bohemian"),
    );
  }
}
